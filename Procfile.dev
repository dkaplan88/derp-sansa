postgres:     bundle exec postgres -D /usr/local/var/postgres
web:          bundle exec thin start -p 3000 -e development
# redis:        bundle exec redis-server /usr/local/etc/redis.conf
# memcached:    bundle exec /usr/local/bin/memcached
# worker:       bundle exec rake resque:work QUEUE=high,low
# scheduler:    bundle exec rake resque:scheduler
# solr:         bundle exec rake sunspot:solr:run
# mailcatcher:  bundle exec mailcatcher -f
