# PostgreSQL. Versions 8.2 and up are supported.
development:
  database: derp_sansa_development
  adapter: postgresql
  encoding: utf8
  host: localhost

test:
  database: derp_sansa_test
  adapter: postgresql
  encoding: utf8
  host: localhost
